1 folder or file per pipeline:

* <project name>.Jenkinsfile
* <project name>/Jenkinsfile

The only reason to use a folder is if there are other files that are required for the project for some reason
